import * as test from './commands/discord-test'
import * as ban from './commands/ban'
import * as punishment from './commands/punishment'

export const commands = {
    test,
    ban,
    punishment
}